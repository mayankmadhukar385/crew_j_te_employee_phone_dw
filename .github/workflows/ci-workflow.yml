name: CI Workflow

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
  push:
    branches:
      - main

jobs:
  ci_job:
    name: CI job
    runs-on: ubuntu-latest

    env:
      PYTHONPATH: .

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Bazel
        uses: bazel-contrib/setup-bazel@0.18.0

      - name: Upgrade pip
        run: |
          python -m pip install --upgrade pip
          python -m pip --version

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install "pytest>=8.0.0" "pytest-html>=4.1.1"
          pip install "pylint>=3.2.0" "nbqa>=1.8.0"

      - name: Verify Bazel workspace
        run: |
          pwd
          ls -la
          test -f MODULE.bazel
          test -f BUILD.bazel
      
      - name: Run Bazel tests
        run: |
          bazel test //tests:tests --test_output=errors --verbose_failures || {
          cat bazel-out/k8-fastbuild/testlogs/tests/tests/test.log || true
          cat /home/runner/.bazel/execroot/_main/bazel-out/k8-fastbuild/testlogs/tests/tests/test.log || true
          exit 1}

      - name: Upload Bazel test logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bazel-testlogs
          path: |
            bazel-testlogs
            bazel-out/**/testlogs/**
