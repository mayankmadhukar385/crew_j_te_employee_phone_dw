# This is a Databricks asset bundle definition for dabproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: crew_j_te_employee_phone_dw
  uuid: 182eccf5-ef99-4c0a-af4a-44bb98a91b40

include:
  - resources/*.yml
  - resources/*/*.yml


# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: dev_${workspace.current_user.short_name}
      source_linked_deployment: false
    workspace:
      host: https://dbc-f258a182-511d.cloud.databricks.com/
    variables:
      catalog: assetbundles
      schema: ${workspace.current_user.short_name}
  stage:
    mode: production
    workspace:
      host: https://dbc-f258a182-511d.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/mayank.madhukar@pwc.com to make sure we only have a single copy.
      root_path: /Workspace/Users/mayank.madhukar@pwc.com/STAGE_Boeing/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: crew_j_te_employee_phone_dw
      schema: Stage
    permissions:
      - user_name: mayank.madhukar@pwc.com
        level: CAN_MANAGE
  prod:
    mode: production
    workspace:
      host: https://dbc-f258a182-511d.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/mayank.madhukar@pwc.com to make sure we only have a single copy.
      root_path: /Workspace/Users/mayank.madhukar@pwc.com/PROD_Boeing/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: crew_j_te_employee_phone_dw
      schema: prod
    permissions:
      - user_name: mayank.madhukar@pwc.com
        level: CAN_MANAGE
