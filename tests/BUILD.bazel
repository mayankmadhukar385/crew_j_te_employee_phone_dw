load("@rules_python//python:defs.bzl", "py_test")

py_test(
    name = "tests",
    srcs = glob(
        ["**/*.py"],
        exclude = ["fixtures/**"],
    ),
    main = "pytest_main.py",
    deps = [
        "//src:src_lib",
        "@pip//pytest",
        "@pip//chispa",
        "@pip//pyspark",
        "@pip//delta_spark",
        "@pip//pyyaml",
    ],
)